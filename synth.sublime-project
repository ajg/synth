{
    "build_systems":
    [
        /* TODO: Use variants instead of this mess. */
        {
            "cmd": "scons && python setup.py clean --all install",
            "shell": true,
            "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
            "name": "Synth: Build All (release)",
            "path": "/usr/local/bin:$PATH",
            "working_dir": "${project_path}"
        },
        {
            "cmd": "scons debug=1 synth",
            "shell": true,
            "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
            "name": "Synth: Build Command-Line Tool (debug)",
            "path": "/usr/local/bin:$PATH",
            "working_dir": "${project_path}"
        },
        {
            "cmd": "scons debug=1 test && ./test && scons debug=1 && python setup.py clean --all install && ls tests/python/binding*.py | xargs -n 1 python",
            "shell": true,
            "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
            "name": "Synth: Build All & Run Tests (debug)",
            "path": "/usr/local/bin:$PATH",
            "working_dir": "${project_path}"
        }
    ],
    "folders":
    [
        {
            "file_exclude_patterns":
            [
                ".sconsign.dblite",
                "MANIFEST",
                "synth",
                "test",
                "*.o",
                "*.o-*",
                "*.os",
                "*.os-*",
                "*.pyc",
                "*.so"
            ],
            "folder_exclude_patterns":
            [
                "build",
                "dist",
                "tests/tut-framework"
            ],
            "follow_symlinks": true,
            "path": "."
        }
    ],
    "settings":
    {
        "tab_size": 4,
        "word_wrap": false
    }
}
